# Лаборатория №1
   Для выполнения лабораторной работы был определен порождающий паттерн проектирования **Абстрактная фабрика** (*Abstract Factory*) .

# Предметная область
   Создание персонажа в компьютерной игре, для которго мы настраиваем снаряжение.
   Персонаж может быть: питомцем, NPC, боссом.

# Описание проблемы
    При создании персонажа есть следующие сложности:
    -Создание персонажа с дополнительными характеристиками, которые нужно настраивать поэтапно.
    -Создания различных вариантов персонажей, характеристики которых могут быть одинаковы, но снаряжение разное. (Например: босс с топором или щитом)

# Решение
   Использование паттерна **Абстрактная фабрика**. Абстрактная фабрика позволяет создавать фабрики, которые производят семейства связанных объектов без явного определения их классов.
    В данном случае:

-**Абстрактная фабрика (AbstractCharacterFactory)** — интерфейс самой фабрики.
-**Абстрактный класс персонажа (AbstractCharacter)** — создаёт персонажей.
-**Абстрактный класс экипировки (AbstractEquipment)** — создаёт экипировку.

-**Конкретные классы персонажей (Pet, Npc, Boss)** — реализуют базовый интерфейс/класс персонажей.
-**Конкретные классы экипировки (MeleeWeapon, RangedWeapon, MagicWeapon)** — реализуют базовый интерфейс/класс экипировки.

-**Конкретные фабрики (PetFactory, NpcFactory, BossFactory, BossWithAxeFactory, BossWithShieldFactory)** — реализуют интерфейс.

-**Клиент (CharacterClient)** — пользовательский интерфейс.

# Диаграмма классов
![Паттерн Абстрактная фабрика](./file.pdf)

# Без паттерна
Создание объектов происходит напрямую, без фабрик.

Минусы: трудно добавлять новые типы или менять логику создания без изменения кода, что усложняет поддержку и расширение.