# Лаборатория №4
# Поведенчиские паттерны
   Для выполнения лабораторной работы был определен архитектурный паттерн проектирования **Коллекция объектов** (*Identity Map*) .

## Предметная область
   Управление коллекцией взаимосвязанных объектов — персонажей и предметов в игре, с целью избегания дублирования объектов и ускорения поиска.

## Описание проблемы
    -При наличии большого числа объектов (персонажей, предметов) необходимо обеспечить эффективное управление ими.
    -Часто возникает необходимость повторного получения уже существующего объекта по его уникальному идентификатору.

## Решение
    Использование паттерна **Коллекция объектов** (*Identity Map*).
    централизованное хранилище объектов, которое обеспечивает: 
    -уникальное хранение каждого объекта по его идентификатору.
    -быстрый доступ к объекту по ID. 

   -**GameObject** — абстрактный класс для игровых объектов.
   -**IdentityMap** — коллекция объектов.  
   -**Character** — пример класса персонажей.
   -**RunPattern1** — основной класс для демонстрации.
   -**DatabaseManager** - класс для работы сбазой данных
   -**БД** - база данных


# Диаграмма классов
![коллекция_объектов](./коллекция_объектов.png)

# Без паттерна
Минусы без паттерна:  
    - Каждый запрос к объекту приводил бы к прямому обращению к базе данных — без проверки, был ли этот объект уже загружен ранее.

    - Дублирование объектов в памяти: один и тот же персонаж мог быть загружен несколько раз, каждый раз создавая новый экземпляр класса.

    - Отсутствие централизованного кэша — нет единого места, где хранятся все загруженные объекты.

    - Неконсистентность данных: изменения объекта в одном месте не отражались бы автоматически в других частях программы, если там используется другой экземпляр того же объекта.

    - Последовательный поиск по коллекции (например, в списке) вместо быстрого доступа по ключу.
